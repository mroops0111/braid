{
  "name": "@braidhq/desktop",
  "type": "module",
  "version": "0.0.1",
  "private": true,
  "description": "Braid Tauri desktop shell. Embeds @braidhq/server as sidecar; reuses @braidhq/studio for UI.",
  "license": "MIT",
  "scripts": {
    "build": "echo 'desktop build runs Tauri in Phase 6'",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "vitest run --passWithNoTests",
    "dev": "echo 'tauri dev (Phase 6)'",
    "clean": "rm -rf dist .turbo *.tsbuildinfo"
  },
  "dependencies": {
    "@braidhq/schema": "workspace:*",
    "@braidhq/server": "workspace:*",
    "@braidhq/studio": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
