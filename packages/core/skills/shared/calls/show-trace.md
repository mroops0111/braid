# `show_trace`

Emit it once, near the top, before the answer.

`skipped` is the part that earns this call, so record it honestly rather than leaving the trail looking tidy.

Leave `audiences` empty. A reader of either kind wants to know what was looked at and what was passed over.
